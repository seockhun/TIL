import React from "react";

interface Props {
  children?: React.ReactNode;
  width: number;
  onClick?: () => void;
}

const App: React.FC<Props> = (props) => {
  const { width, children, onClick } = porps;
}